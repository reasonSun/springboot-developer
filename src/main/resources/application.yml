spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true

  spring:
    datasource:
      url: jdbc:h2:tcp://localhost/~/test2
      username: sa

    h2:
      console:
        enabled: true